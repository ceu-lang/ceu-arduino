#include "arduino.ceu"

native do
    ##include "EEPROM.h"
    ##include "SPI.h"
    ##include "GD2.h"
end

_GD.begin();

loop i do
    _GD.ClearColorRGB(0x103000);
    _GD.Clear();
    _GD.cmd_text(240, 136, 31, _OPT_CENTER, "Hello world");
    _GD.swap();
    await 10ms;
end