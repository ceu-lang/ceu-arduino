native/pre do
    ##define CEU_FEATURES_ISR_SLEEP
end

#include "arduino/isr/timer.ceu"
#include "arduino/isr/adc.ceu"

output on/off PIN_13;

loop do
    await 1s;
    var int value = await Adc(_A0);
    if value > 1000 then
        emit PIN_13(on);
    else
        emit PIN_13(off);
    end
end
