native/pre do
    //##define CEU_FEATURES_ISR_SLEEP
end

#include "arduino/isr/timer.ceu"

output bool PIN_13;

loop do
    await 500ms;
    emit PIN_13(on);
    await 500ms;
    emit PIN_13(off);
end
